- if @offers.empty?
  .no_offers No offers

- else
  - @offers.each do |offer|
    .offer
      .title=     offer["title"]
      .payout=    offer["payout"]
      .thumbnail
        %img{ src: offer["thumbnail"]["lowres"] }
